{
  "name": "conversation-relay-functions",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "twilio": "5.3.5",
    "@twilio/runtime-handler": "2.0.0"
  },
  "devDependencies": {
    "twilio-run": "^4.0.0"
  },
  "engines": {
    "node": "18"
  }
}